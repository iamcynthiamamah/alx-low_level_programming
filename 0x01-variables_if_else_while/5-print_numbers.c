#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always return 0 (successful)
 */

int main(void)
{
	int n;

	for (n = 1; n < 10; n++)
	{
		printf("%d", n);
	}

	printf("\n");
	return (0);

}
