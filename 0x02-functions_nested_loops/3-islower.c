#include "main.h"

/**
 * _islower - Checks code in lowercase
 *
 * @c: basics
 * Return: Always 0
 */

int _islower(int c)
{
	int islower(int c);

	if (islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
