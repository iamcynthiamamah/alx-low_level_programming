#include "main.h"

/**
 * _isalpha - prints alpha
 * @c: Description
 * Return: Always return 0
 */

int _isalpha(int c)
{
	int isalpha(int c);

	if isalpha(int c)
	{
		return (1);
	}
	else
	{
		return (0);
